import { RolePermissionDirective } from './role-permission.directive';

describe('RolePermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new RolePermissionDirective();
    expect(directive).toBeTruthy();
  });
});
